# HandbookSearch Environment Variables Example
# DEPRECATED: Use SecureStore instead for secrets management.
# See README.md for SecureStore setup instructions.

# Only non-secret configuration here
ASPNETCORE_ENVIRONMENT=Production

# Note: Database password, API keys, and other secrets should be stored in SecureStore:
# ~/.config/handbook-search/secrets/secrets.json
#
# Setup SecureStore:
#   SecureStore create -s ~/.config/handbook-search/secrets/secrets.json \
#     -k ~/.config/handbook-search/keys/secrets.key
#
#   SecureStore set -s ~/.config/handbook-search/secrets/secrets.json \
#     -k ~/.config/handbook-search/keys/secrets.key "Database:Password=your_password"
#   SecureStore set -s ~/.config/handbook-search/secrets/secrets.json \
#     -k ~/.config/handbook-search/keys/secrets.key "AzureTranslator:ApiKey=your_api_key"
